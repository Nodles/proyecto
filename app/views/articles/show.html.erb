<p>
  <b>Title:</b>
  <%= @article.title %>
</p>
<p>
  <b>Body:</b>
  <%= @article.body %>
</p>
	
	<% if current_user %>
	<%=  current_user.email %>
		<% if admin? %>
			<%= link_to 'marca', 'www.marca.com'  %>
		<% end %>
	<% end %>
<%= @si = "si"
	@no = "no"
	current_user
	if current_user	
		email_comprobacion = "1"
		email = current_user.id 
	end %>
	 	<%= link_to 'Destroy', @article, :confirm => 'Are you sure?', :method => :delete %>
	 	<%= link_to 'Back', articles_path %>
	 	<%= link_to_if email_comprobacion == email, 'edit', edit_article_path(@article) %>	
	
<%= link_to 'Show', @article; %> 
<%=  link_to 'Edit', edit_article_path(@article) %>
<%= link_to 'Destroy', @article, :confirm => 'Are you sure?', :method => :delete %>
<%= link_to 'Back', articles_path %>
<%=	puts "si" %>

<h2>Comments</h2>

<%= form_for([@article, @article.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <p><%= f.submit %></p>
<% end %>